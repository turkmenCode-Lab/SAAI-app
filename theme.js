import { DefaultTheme } from '@react-navigation/native';

export const AppTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        background: '#000',
        text: '#ffffff',
        primary: '#A1A1AA',
    },
};
