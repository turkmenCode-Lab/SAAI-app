import { DefaultTheme } from '@react-navigation/native';

export const AppTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        background: '#1E1E2E',
        text: '#ffffff',
        primary: '#FF6B00',
    },
};
